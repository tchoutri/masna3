services:
  devel:
    user: ${UID}:${GID}
    image: $USER-masna3-devel
    build:
      dockerfile: Dockerfile
      target: devel
      args:
        - USER=${USER}
        - UID=${UID}
        - GID=${GID}
    ports:
      - "8085:8085"
      - "8086:8086"
    volumes:
      - $SOURCE_DIRECTORY:/home/$USER/masna3:Z,U
      - database-data:/home/$USER/masna3/pgdata
    command: sleep infinity
    profiles:
      - local
    depends_on:
      - masna3-database
    working_dir: /home/$USER/masna3
    init: true

  masna3-database:
    image: docker.io/library/postgres:18
    container_name: masna3-database
    user: postgres
    healthcheck:
      test: [ "CMD-SHELL", "pg_isready" ]
      interval: 10s
      timeout: 5s
      retries: 5
    ports:
      - "5433:5432"
    expose:
      - "5432"
    env_file:
      - database.env
    volumes:
      - database-data:/var/lib/postgresql/docker
    profiles:
      - local

  s3mock:
    image: adobe/s3mock:4.7.0
    environment:
      - COM_ADOBE_TESTING_S3MOCK_STORE_RETAIN_FILES_ON_EXIT=true
      - COM_ADOBE_TESTING_S3MOCK_STORE_ROOT=containers3root
      - COM_ADOBE_TESTING_S3MOCK_STORE_INITIAL_BUCKETS=masna3-dev
    ports:
      - 9090:9090
    volumes:
      - s3mock-data:/containers3root

volumes:
  database-data:
  s3mock-data:
